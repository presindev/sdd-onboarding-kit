<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Architecture — {{PROJECT_NAME}}</title>
  <style>
    :root {
      --c-accent: #2563eb; --c-accent-bg: #eff6ff;
      --c-border: #d1d5db; --c-bg-alt: #f9fafb;
      --c-text: #111827; --c-text-muted: #6b7280;
      --font-sans: system-ui, -apple-system, sans-serif;
      --font-mono: ui-monospace, Menlo, monospace;
    }
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: var(--font-sans); font-size: 15px; line-height: 1.65; color: var(--c-text); max-width: 860px; margin: 0 auto; padding: 2rem 1.5rem; }
    h1 { font-size: 1.55rem; margin-bottom: 0.35rem; }
    .subtitle { color: var(--c-text-muted); font-size: 0.9rem; margin-bottom: 1.5rem; }
    h2 { font-size: 1.1rem; font-weight: 700; margin: 2rem 0 0.65rem; padding-bottom: 0.3rem; border-bottom: 1px solid var(--c-border); }
    h3 { font-size: 0.95rem; font-weight: 600; margin: 1.1rem 0 0.4rem; }
    p { margin: 0.6rem 0; }
    ul, ol { padding-left: 1.5rem; margin: 0.5rem 0; }
    li { margin: 0.25rem 0; }
    table { width: 100%; border-collapse: collapse; font-size: 0.875rem; margin: 1rem 0; }
    th, td { border: 1px solid var(--c-border); padding: 0.5rem 0.75rem; text-align: left; }
    th { background: var(--c-bg-alt); font-weight: 600; }
    code { font-family: var(--font-mono); font-size: 0.875em; background: #e5e7eb; padding: 0.1em 0.4em; border-radius: 3px; }
    pre { background: #1e1e2e; color: #cdd6f4; padding: 1rem 1.25rem; border-radius: 4px; overflow-x: auto; font-family: var(--font-mono); font-size: 0.84rem; margin: 1rem 0; }
    pre code { background: none; padding: 0; }
    .diagram-container { border: 1px solid var(--c-border); border-radius: 4px; padding: 1.5rem; margin: 1rem 0; background: var(--c-bg-alt); text-align: center; overflow-x: auto; }
    .note { border: 1px dashed var(--c-accent); border-radius: 4px; padding: 0.75rem 1rem; background: var(--c-accent-bg); font-size: 0.85rem; margin: 1rem 0; }
    hr { border: none; border-top: 1px solid var(--c-border); margin: 1.5rem 0; }
  </style>
</head>
<body>

  <h1>Architecture — {{PROJECT_NAME}}</h1>
  <p class="subtitle">Claude Code should read this file before creating or reviewing specs.</p>

  <hr>

  <section id="system-overview">
    <h2>System overview</h2>
    <p>TODO: Describe the system architecture.</p>

    <div class="diagram-container">
      <svg width="540" height="160" viewBox="0 0 540 160" aria-label="System overview diagram placeholder">
        <defs>
          <marker id="arr" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto">
            <polygon points="0 0, 8 3, 0 6" fill="#6b7280"/>
          </marker>
        </defs>
        <rect x="20"  y="55" width="120" height="50" rx="6" fill="#eff6ff" stroke="#2563eb" stroke-width="1.5"/>
        <text x="80"  y="85" text-anchor="middle" font-family="sans-serif" font-size="12" fill="#1e3a8a">Client</text>
        <line x1="140" y1="80" x2="198" y2="80" stroke="#6b7280" stroke-width="1.5" marker-end="url(#arr)"/>
        <rect x="200" y="45" width="140" height="70" rx="6" fill="#f5f3ff" stroke="#7c3aed" stroke-width="1.5"/>
        <text x="270" y="85" text-anchor="middle" font-family="sans-serif" font-size="12" fill="#4c1d95">Application</text>
        <line x1="340" y1="80" x2="398" y2="80" stroke="#6b7280" stroke-width="1.5" marker-end="url(#arr)"/>
        <rect x="400" y="55" width="120" height="50" rx="6" fill="#eafaf1" stroke="#27ae60" stroke-width="1.5"/>
        <text x="460" y="85" text-anchor="middle" font-family="sans-serif" font-size="12" fill="#14532d">Data store</text>
        <text x="270" y="148" text-anchor="middle" font-family="sans-serif" font-size="11" fill="#9ca3af">TODO: replace with actual architecture diagram</text>
      </svg>
    </div>
  </section>

  <section id="main-modules">
    <h2>Main modules</h2>
    <p>TODO: List important modules / packages and responsibilities.</p>
    <table>
      <thead>
        <tr><th>Module / Package</th><th>Responsibility</th><th>Key files</th></tr>
      </thead>
      <tbody>
        <tr><td>TODO</td><td>TODO</td><td>TODO</td></tr>
      </tbody>
    </table>
  </section>

  <section id="data-model">
    <h2>Data model</h2>
    <p>TODO: Describe important entities, schemas or persistence choices.</p>
  </section>

  <section id="external-integrations">
    <h2>External integrations</h2>
    <p>TODO: Describe APIs, queues, databases, auth providers or third-party systems.</p>
    <table>
      <thead>
        <tr><th>System</th><th>Purpose</th><th>Protocol / SDK</th></tr>
      </thead>
      <tbody>
        <tr><td>TODO</td><td>TODO</td><td>TODO</td></tr>
      </tbody>
    </table>
  </section>

  <section id="constraints">
    <h2>Constraints</h2>
    <p>TODO: Describe performance, security, compliance, deployment or compatibility constraints.</p>
  </section>

  <section id="decisions">
    <h2>Decisions</h2>
    <p>TODO: Link ADRs or record key architecture decisions.</p>
    <div class="note">
      <strong>Note for Claude Code:</strong> reference architecture decisions here before choosing implementation approaches in <code>design.html</code>.
    </div>
  </section>

</body>
</html>
