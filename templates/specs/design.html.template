<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Design — {{FEATURE_TITLE}}</title>
  <link rel="stylesheet" href="spec.css">
</head>
<body>
<div class="page-wrap">

  <nav class="sidebar" aria-label="Table of contents">
    <p class="sidebar-title">Contents</p>
    <div id="toc-list"></div>
  </nav>

  <div class="main-content">
    <header class="spec-header">
      <h1>Design — {{FEATURE_TITLE}}</h1>
      <table class="meta-table">
        <tbody>
          <tr><th>Task ID</th>      <td><code>{{TASK_ID}}</code></td></tr>
          <tr><th>Feature slug</th> <td><code>{{FEATURE_SLUG}}</code></td></tr>
          <tr><th>Status</th>       <td><span class="badge badge-draft">spec_draft</span></td></tr>
        </tbody>
      </table>
    </header>

    <main>

      <section id="technical-summary">
        <h2>Technical summary</h2>
        <p>TODO: Explain the chosen implementation approach.</p>
      </section>

      <section id="data-flow">
        <h2>Data flow</h2>
        <p>TODO: Replace the placeholder below with an inline SVG diagram describing the main data flow.</p>
        <div class="diagram-container">
          <svg width="560" height="140" viewBox="0 0 560 140" aria-label="Data flow diagram — replace with actual diagram">
            <defs>
              <marker id="arrowhead" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto">
                <polygon points="0 0, 8 3, 0 6" fill="#6b7280"/>
              </marker>
            </defs>
            <!-- Input box -->
            <rect x="20" y="50" width="120" height="40" rx="6" fill="#eff6ff" stroke="#2563eb" stroke-width="1.5"/>
            <text x="80" y="75" text-anchor="middle" font-family="sans-serif" font-size="12" fill="#1e3a8a">Input</text>
            <!-- Arrow -->
            <line x1="140" y1="70" x2="218" y2="70" stroke="#6b7280" stroke-width="1.5" marker-end="url(#arrowhead)"/>
            <!-- Process box -->
            <rect x="220" y="40" width="120" height="60" rx="6" fill="#f5f3ff" stroke="#7c3aed" stroke-width="1.5"/>
            <text x="280" y="75" text-anchor="middle" font-family="sans-serif" font-size="12" fill="#4c1d95">Process</text>
            <!-- Arrow -->
            <line x1="340" y1="70" x2="418" y2="70" stroke="#6b7280" stroke-width="1.5" marker-end="url(#arrowhead)"/>
            <!-- Output box -->
            <rect x="420" y="50" width="120" height="40" rx="6" fill="#eafaf1" stroke="#27ae60" stroke-width="1.5"/>
            <text x="480" y="75" text-anchor="middle" font-family="sans-serif" font-size="12" fill="#14532d">Output</text>
            <!-- Caption -->
            <text x="280" y="128" text-anchor="middle" font-family="sans-serif" font-size="11" fill="#9ca3af">TODO: replace with actual data flow</text>
          </svg>
        </div>
      </section>

      <section id="project-map-references">
        <h2>Project map references</h2>
        <p>TODO: Which areas of the project map does this change touch (modules, entrypoints, protected areas)? Reference the map instead of re-describing the structure; if the change will make the map stale, add the map update to the closing tasks. State <code>None</code> if the project has no map.</p>
      </section>

      <section id="files-to-change">
        <h2>Files to change</h2>
        <table>
          <thead>
            <tr><th>File</th><th>Change</th><th>Reason</th></tr>
          </thead>
          <tbody>
            <tr><td>TODO</td><td>TODO</td><td>TODO</td></tr>
          </tbody>
        </table>
      </section>

      <section id="files-not-to-change">
        <h2>Files not to change</h2>
        <table>
          <thead>
            <tr><th>File or directory</th><th>Reason</th></tr>
          </thead>
          <tbody>
            <tr><td>TODO</td><td>TODO</td></tr>
          </tbody>
        </table>
      </section>

      <section id="interfaces-and-contracts">
        <h2>Interfaces and contracts</h2>
        <p>TODO: Describe APIs, functions, classes, schemas or command interfaces.</p>

        <div class="tabs">
          <ul class="tab-bar">
            <li><button class="tab-btn" type="button">API / Functions</button></li>
            <li><button class="tab-btn" type="button">Schema</button></li>
            <li><button class="tab-btn" type="button">CLI</button></li>
          </ul>
          <div class="tab-panel">
            <pre><code>// TODO: paste interface or function signature here</code></pre>
          </div>
          <div class="tab-panel">
            <pre><code>// TODO: paste schema definition here</code></pre>
          </div>
          <div class="tab-panel">
            <pre><code># TODO: paste CLI usage here</code></pre>
          </div>
        </div>
      </section>

      <section id="data-model-changes">
        <h2>Data model changes</h2>
        <p>TODO: Describe schema, migration or persistence changes. If none, state <code>None</code>.</p>
      </section>

      <section id="external-dependencies-and-freshness">
        <h2>External dependencies and freshness</h2>
        <p>TODO: If this change touches external dependencies, SDKs, APIs, framework configuration, or security-sensitive integrations, verify current official documentation and record the evidence below. If the change is purely internal, state <code>None</code>. Verification is mandatory for high-risk categories: auth, payments, database migrations, cloud infrastructure, framework upgrades, security-sensitive code.</p>
        <table>
          <thead>
            <tr><th>External system / dependency</th><th>Docs checked (source, date)</th><th>Version constraints</th><th>Deprecated APIs avoided</th><th>Compatibility notes</th></tr>
          </thead>
          <tbody>
            <tr><td>TODO</td><td>TODO</td><td>TODO</td><td>TODO</td><td>TODO</td></tr>
          </tbody>
        </table>
      </section>

      <section id="security-and-permissions">
        <h2>Security and permissions</h2>
        <p>TODO: If this change touches trust boundaries, authentication/authorization, secrets handling, input validation on external data, or operations that need explicit permission (destructive commands, remote mutations, protected files) — describe each and how it is gated. High-risk categories trigger the deep-review escalation ladder (<code>reference/deep-review-policy.md</code>). If none apply, state <code>None</code>.</p>
      </section>

      <section id="documentation-targets">
        <h2>Documentation targets</h2>
        <p>TODO: Which documentation will this design affect when implemented (README, API docs, setup docs, changelog, ADRs, migration notes, project map)? List the targets or state <code>None expected</code>. The reviewer confirms the final list in the review's <code>Documentation decision</code>.</p>
      </section>

      <section id="test-design">
        <h2>Test design</h2>
        <p>TODO: Explain which tests will be added or updated and how they map to requirements.</p>
      </section>

      <section id="ui-verification-plan">
        <h2>UI verification plan</h2>
        <p>TODO: If the change affects a browser UI, describe how its acceptance criteria will be verified — the <code>browser-tester</code> agent (when installed) or the manual <code>ui-qa</code> procedure: routes to open, states to reach, screenshots to compare, viewports to check. If no UI is affected, state <code>None</code>. Never include credentials; local/test instances only.</p>
      </section>

      <section id="risks-and-trade-offs">
        <h2>Risks and trade-offs</h2>
        <p>TODO: List risks, rejected alternatives and trade-offs.</p>
      </section>

      <section id="rollback-plan">
        <h2>Rollback plan</h2>
        <p>TODO: Explain how to revert or disable the change if needed.</p>
      </section>

    </main>
  </div><!-- /.main-content -->
</div><!-- /.page-wrap -->
<script src="spec.js"></script>
</body>
</html>
