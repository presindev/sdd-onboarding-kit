<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Requirements — {{FEATURE_TITLE}}</title>
  <link rel="stylesheet" href="spec.css">
</head>
<body>
<div class="page-wrap">

  <nav class="sidebar" aria-label="Table of contents">
    <p class="sidebar-title">Contents</p>
    <div id="toc-list"></div>
  </nav>

  <div class="main-content">
    <header class="spec-header">
      <h1>Requirements — {{FEATURE_TITLE}}</h1>
      <table class="meta-table">
        <tbody>
          <tr><th>Task ID</th>       <td><code>{{TASK_ID}}</code></td></tr>
          <tr><th>Feature slug</th>  <td><code>{{FEATURE_SLUG}}</code></td></tr>
          <tr><th>Status</th>        <td><span class="badge badge-draft">spec_draft</span></td></tr>
          <tr><th>Author</th>        <td>spec-author</td></tr>
          <tr><th>Human approval</th><td><span class="badge badge-pending">pending</span></td></tr>
        </tbody>
      </table>
    </header>

    <main>

      <section id="summary">
        <h2>Summary</h2>
        <p>{{FEATURE_SUMMARY}}</p>
      </section>

      <section id="functional-requirements">
        <h2>Functional requirements</h2>
        <p>Use the configured requirements format.</p>

        <h3 id="ears-example">EARS example</h3>
        <div class="req-row">
          <span class="req-id">REQ-001</span>
          <span>When <code>&lt;trigger&gt;</code>, the system shall <code>&lt;expected response&gt;</code>.</span>
        </div>
        <div class="req-row">
          <span class="req-id">REQ-002</span>
          <span>If <code>&lt;condition&gt;</code>, the system shall <code>&lt;expected response&gt;</code>.</span>
        </div>
        <div class="req-row">
          <span class="req-id">REQ-003</span>
          <span>While <code>&lt;state&gt;</code>, the system shall <code>&lt;expected response&gt;</code>.</span>
        </div>
      </section>

      <section id="non-functional-requirements">
        <h2>Non-functional requirements</h2>
        <div class="req-row nfr">
          <span class="req-id nfr">NFR-001</span>
          <span>TODO.</span>
        </div>
      </section>

      <section id="edge-cases">
        <h2>Edge cases</h2>
        <div class="req-row edge">
          <span class="req-id edge">EDGE-001</span>
          <span>TODO.</span>
        </div>
      </section>

      <section id="error-states">
        <h2>Error states</h2>
        <div class="req-row err">
          <span class="req-id err">ERR-001</span>
          <span>TODO.</span>
        </div>
      </section>

      <section id="acceptance-criteria">
        <h2>Acceptance criteria</h2>
        <div class="req-row ac">
          <span class="req-id ac">AC-001</span>
          <span>TODO.</span>
        </div>
      </section>

      <section id="ui-acceptance-criteria">
        <h2>UI acceptance criteria</h2>
        <p>TODO: Fill only when the change affects a browser UI; otherwise state <code>None</code>. Screenshots and mockups are evidence, not complete requirements — ask when the visual intent is ambiguous. Never record credentials here: login/auth constraints name the account type (e.g. <code>test account with admin role</code>), never secrets.</p>
        <table>
          <thead>
            <tr><th>ID</th><th>Target route</th><th>Expected visual state</th><th>Interaction flow</th><th>Accessibility checks</th><th>Responsive states</th><th>Login/auth constraints</th></tr>
          </thead>
          <tbody>
            <tr><td><span class="req-id ac">UI-001</span></td><td>TODO</td><td>TODO</td><td>TODO</td><td>TODO</td><td>TODO</td><td>TODO</td></tr>
          </tbody>
        </table>
        <p>Screenshots/mockups provided: TODO (file names or links, or <code>None</code>).</p>
      </section>

      <section id="visual-evidence">
        <h2>Visual evidence</h2>
        <p>TODO: Fill only when screenshots, mockups, or other images are inputs for this task; otherwise state <code>None</code>. Screenshots are evidence, not automatically complete requirements — ask when the visual intent is ambiguous. If an image contains secrets or personal data, flag it and never transcribe them here.</p>
        <table>
          <thead>
            <tr><th>ID</th><th>Image (file/link)</th><th>Shows</th><th>Provenance (route, environment, viewport)</th><th>Intent</th></tr>
          </thead>
          <tbody>
            <tr><td><span class="req-id ac">IMG-001</span></td><td>TODO</td><td>TODO</td><td>TODO (or unknown)</td><td>design to build / bug evidence / regression before-after / acceptance criterion</td></tr>
          </tbody>
        </table>
        <p><strong>Affected routes/components:</strong> TODO.</p>
        <p><strong>Observed behavior</strong> (what the images show, facts only): TODO.</p>
        <p><strong>Expected behavior</strong> (confirmed intent — not inferred from pixels alone): TODO.</p>
        <p>Testable visual expectations, accessibility concerns, and responsive behavior go to the <a href="#ui-acceptance-criteria">UI acceptance criteria</a> table, referencing the image IDs above. Unresolved visual questions go to <code>open-questions.html</code>.</p>
      </section>

      <section id="external-dependencies">
        <h2>External dependencies and APIs</h2>
        <p>TODO: List the external systems, SDKs, APIs, or framework features this feature interacts with — or state <code>None</code>. This is the requirements-level inventory; the verification evidence (docs checked, versions, deprecations) belongs in the design's <code>External dependencies and freshness</code> section.</p>
      </section>

      <section id="documentation-expectations">
        <h2>Documentation expectations</h2>
        <p>TODO: Which documentation is this feature expected to affect (README, API docs, setup docs, changelog, user-facing docs)? State <code>None expected</code> if none — the reviewer makes the final documentation decision either way.</p>
      </section>

      <section id="memory-decision-implications">
        <h2>Memory and decision implications</h2>
        <p>TODO: Will this feature settle a significant decision (architecture choice, rejected alternative, workflow rule) that belongs in the decision log? Name the expected candidates or state <code>None</code>. Entries are proposed at review time, never written without developer approval; trivial decisions are not logged.</p>
      </section>

      <section id="requirement-to-test-mapping">
        <h2>Requirement-to-test mapping</h2>
        <table>
          <thead>
            <tr>
              <th>Requirement</th>
              <th>Expected test(s)</th>
              <th>Notes</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><span class="req-id">REQ-001</span></td>
              <td>TODO</td>
              <td>TODO</td>
            </tr>
          </tbody>
        </table>
      </section>

    </main>
  </div><!-- /.main-content -->
</div><!-- /.page-wrap -->
<script src="spec.js"></script>
</body>
</html>
